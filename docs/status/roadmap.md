# Roadmap

## Immediate

1. Build and publish this documentation system through GitHub Pages.
2. Run read-only DNS zone checks.
3. Run SSL status checks for `test.securedme.ca`.
4. Generate a deployment plan for `public_html/test.securedme.ca`.

## Next

1. Add an email inventory test.
2. Add a mailbox creation test with an env-backed password key.
3. Add a SecuredMe Education domain mismatch report.
4. Add a public placeholder deployment report.

## Later

1. Add a deletion runbook without executing deletion.
2. Add DNS mutation tests only after a rollback strategy exists.
3. Add automated redaction checks for published reports.

